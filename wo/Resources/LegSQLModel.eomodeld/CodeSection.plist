{
    attributes = (
        {
            columnName = name; 
            externalType = varchar; 
            name = name; 
            valueClassName = NSString; 
            width = 50; 
        }, 
        {
            columnName = pk; 
            externalType = INT; 
            name = pk; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (name, pk); 
    className = "org.ganymede.leginfo.eo.CodeSection"; 
    classProperties = (bills, name); 
    externalName = "code_sections"; 
    fetchSpecificationDictionary = {}; 
    name = CodeSection; 
    primaryKeyAttributes = (pk); 
    relationships = (
        {definition = "codeSectionJoins.bill"; name = bills; }, 
        {
            destination = CodeSectionJoin; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = sectionPk; sourceAttribute = pk; }); 
            name = codeSectionJoins; 
        }
    ); 
}

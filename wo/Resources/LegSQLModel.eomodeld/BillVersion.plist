{
    attributes = (
        {
            columnName = "bill_pk"; 
            externalType = int; 
            name = billPk; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = file; 
            externalType = varchar; 
            name = file; 
            valueClassName = NSString; 
            width = 128; 
        }, 
        {
            columnName = "file_date"; 
            externalType = CHAR; 
            name = fileDate; 
            valueClassName = NSString; 
            width = 10; 
        }, 
        {
            columnName = kind; 
            externalType = varchar; 
            name = kind; 
            valueClassName = NSString; 
            width = 32; 
        }, 
        {
            columnName = pk; 
            externalType = INT; 
            name = pk; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (billPk, pk); 
    className = "org.ganymede.leginfo.eo.BillVersion"; 
    classProperties = (bill, file, fileDate, kind, woulds); 
    externalName = "bill_versions"; 
    fetchSpecificationDictionary = {}; 
    name = BillVersion; 
    primaryKeyAttributes = (pk); 
    relationships = (
        {
            destination = Bill; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = pk; sourceAttribute = billPk; }); 
            name = bill; 
        }, 
        {
            destination = BillWould; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = versionPk; sourceAttribute = pk; }); 
            name = woulds; 
        }
    ); 
}
